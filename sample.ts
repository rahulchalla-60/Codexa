// sample.ts
function calculateBlastRadius(depth: number) {
  return depth * 2;
}

function adminDashboard() {
  calculateBlastRadius(5);
}

function userDashboard() {
  calculateBlastRadius(2);
}

function apiWebhook() {
  calculateBlastRadius(10);
}
